apiVersion: v1
kind: Service
metadata:
  name: ai-agent-service
  namespace: airbnb-system
  labels:
    app: ai-agent
spec:
  type: LoadBalancer
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: ai-agent

